import type { LaunchSpec } from './types.ts';

export const VSCODE = '.vscode';
export const LAUNCH_DEFAULT: LaunchSpec = { version: '0.2.0', configurations: [] };
export const LAUNCH_CONFIG = 'launch.config.json';
